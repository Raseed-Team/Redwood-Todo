const TodoApp = () => {
  return (
    <div>
      <p>
        this will have a generate category for a todo card, one textfield input
        form to create a category
      </p>
    </div>
  )
}

export default TodoApp
